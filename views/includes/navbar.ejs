<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top py-1 shadow-sm" id="mainNavbar">
  <div class="container-fluid">
    <a href="/images/venuevista.png">
      <img class="logo" src="/images/venuevista.png" alt="VenueVista Logo" style="height: 70px;" />
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <!-- <a class="nav-link fw-semibold" href="/listings">Explore</a> -->
      </div>
      <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search">
          <input class="form-control me-2 search-inp" type="search" placeholder="Search destinations"
            aria-label="Search" />
          <button class="btn btn-search" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>Search
          </button>
        </form>
      </div>
      <div class="navbar-nav ms-auto align-items-center">
        <a class="nav-link fw-semibold" href="/listings/new">Add your Venue</a>
        <% if(!currUser) { %>
          <a class="nav-link fw-bold text-dark" href="/signup">SignUp</a>
          <a class="nav-link fw-bold text-dark" href="/login">Login</a>
          <% } %>
            <% if(currUser) { %>
              <a class="nav-link fw-bold text-dark" href="/logout">Logout</a>
              <% } %>
      </div>
    </div>
  </div>
</nav>